apiVersion: v1
kind: Secret
metadata:
  name: qsdm-secrets
  namespace: qsdm
type: Opaque
stringData:
  # TLS certificates (base64 encoded)
  # In production, use proper certificate management
  # tls_cert: ""
  # tls_key: ""
  
  # API keys (if needed) — local example placeholder only
  # api_key: "Charming123"
  
  # Database credentials (if using ScyllaDB)
  # scylla_username: ""
  # scylla_password: "Charming123"

