# Submesh profile: load at node startup with [network] submesh_config in qsdmplus.toml
# or env QSDMPLUS_SUBMESH_CONFIG (path may be relative to the main config file).

name = "micropayments"
fees = 0.001
priority = 5
geo_tags = ["US", "EU"]

[parameters]
max_tx_size = "1024"
