console.error(
  'Direct electron-builder publishing is disabled. Build verified Windows and Linux artifacts, then use QSDM/deploy/scripts/publish_hive_release.sh from the release host.'
);
process.exit(1);
