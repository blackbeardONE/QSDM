# NVIDIA NGC–aligned sidecar (see ../../nvidia_locked_qsdm_blockchain_architecture.md)
#
# CPU stack (small image):
#   docker compose up --build
#
# GPU stack (nvcr.io/pytorch; requires `docker login nvcr.io` — see README):
#   docker compose --profile gpu up --build
#   Or: scripts/run-gpu.ps1 (Windows) / scripts/run-gpu.sh (Unix)
#
services:
  gossip:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["python3", "/app/gossip_daemon.py"]
    ports:
      - "9910:9910/udp"
    environment:
      QSDM_MESH_PORT: "9910"

  validator-cpu:
    build:
      context: .
      dockerfile: Dockerfile
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      QSDM_GOSSIP_PEERS: "gossip:9910"
      # QSDM_NGC_REPORT_URL / QSDM_NGC_REPORT_URL — see scripts/wire-qsdm.ps1 or wire-qsdm.sh
      # QSDM_NGC_INGEST_SECRET — must match the node
      # QSDM_NGC_PROOF_NODE_ID — optional; must match node QSDM_NVIDIA_LOCK_EXPECTED_NODE_ID when lock binding is on
    depends_on:
      - gossip

  validator-gpu:
    profiles: ["gpu"]
    build:
      context: .
      dockerfile: Dockerfile.ngc
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      QSDM_GOSSIP_PEERS: "gossip:9910"
      # Same NGC env vars as validator-cpu when pushing proofs to the node (GPU profile for NVIDIA-lock).
    depends_on:
      - gossip
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
