# Copy to ngc.env (gitignored) — never commit real keys.
# NGC CLI API key from https://ngc.nvidia.com/
NGC_CLI_API_KEY=Charming123
# After filling the key: .\scripts\verify-ngc-docker.ps1 (from this app folder)

# Optional: push proof bundles into a running QSDM node (secret must match node's QSDM_NGC_INGEST_SECRET)
# QSDM_NGC_REPORT_URL=http://host.docker.internal:8080/api/v1/monitoring/ngc-proof
# QSDM_NGC_INGEST_SECRET=Charming123
# Optional: include qsdm_node_id in proof JSON when node uses QSDM_NVIDIA_LOCK_EXPECTED_NODE_ID
# QSDM_NGC_PROOF_NODE_ID=my-validator-1
# QSDM_NGC_PROOF_HMAC_SECRET=Charming123
# When node has nvidia_lock_require_ingest_nonce: QSDM_NGC_FETCH_CHALLENGE=true (GET /ngc-challenge before each bundle; 429 uses Retry-After, retries up to QSDM_NGC_CHALLENGE_MAX_RETRIES, default 4)
# Optional: QSDM_NGC_CHALLENGE_JITTER_MAX_SEC=8  # random sleep 0..8s before each challenge (NAT / many validators)
